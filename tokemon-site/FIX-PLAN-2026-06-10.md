# Fix Plan — UI/UX Review 2026-06-10

Branch: `fix/ui-ux-review-2026-06`. Source: `UI-UX-REVIEW-2026-06-10.md`.
Ordered into 9 batches, each independently committable. Verify with `npm run build` + Playwright e2e after each batch; visual spot-checks at 375px and 1280px via dev server.

---

## Batch 1 — Shared chrome + design tokens (unblocks everything else)

1. **Define tokens** in `src/app/globals.css` `@theme`:
   `--color-border: #1f1f1f` (replaces #1a1a1a/#222/#252525 card borders), `--color-border-strong: #333`, `--color-card: #111`, `--color-accent: #e8853b`, `--color-accent-soft: #f0a060`, `--color-text-secondary: #999`, `--color-text-tertiary: #8a8a8a`.
2. **Create `src/components/SiteNav.tsx` + `SiteFooter.tsx`** based on the landing nav/footer (`src/app/page.tsx:177-190, 516-518`). Mobile: keep Blog visible (no hamburger needed — logo, Blog, Download fit at 375px). Render in root `layout.tsx` is NOT possible without restructuring the landing hero overlap — instead import into every page (landing, blog index, blog post via BlogLayout, tag, compare index, 3 tool pages). Fixes: blog-index dead end, keychain nav drift, mobile-nav-Download-only.
3. **Delete dead components**: `src/components/{Hero,Nav,Footer,FeaturesGrid,FeatureSection,InAction}.tsx` (verify no imports first: `grep -r "components/Hero" src/`).
4. **Sweep `text-[#666]` → `text-[#8a8a8a]`** (or token) for small text site-wide; raise InteractiveDemo hint (`InteractiveDemo.tsx:414`) to `text-xs text-white/60`.

Commit: `fix(site): shared nav/footer, design tokens, contrast sweep, remove dead components`

## Batch 2 — Failure states

1. **`src/app/not-found.tsx`** — dark-styled, h1 "Page not found", links to `/`, `/blog`, `/compare`, uses SiteNav/SiteFooter.
2. **`src/app/error.tsx`** — client component, minimal dark page with "Try again" (`reset()`) and link home.

Commit: `fix(site): branded 404 and error pages`

## Batch 3 — Mobile overflow (highest-impact one-liners)

1. **`mdx-components.tsx`**: add `table: (props) => <div className="overflow-x-auto"><table {...props} /></div>`.
2. **`src/app/claude-code-cost/page.tsx:140`**: pricing table wrapper `overflow-hidden` → outer rounded div + inner `overflow-x-auto`.
3. **`src/app/TerminalInstall.tsx:16`**: `overflow-x-hidden` → `overflow-x-auto`; add `max-sm:text-[11px]`; add copy-to-clipboard button (client; clipboard API with "Copied" state, `aria-label="Copy install command"`); render full command statically when reduced-motion (see Batch 4).

Commit: `fix(site): mobile overflow — tables scroll, install command readable + copyable`

## Batch 4 — Motion & WebGL safety

1. **Shared hook `src/lib/useReducedMotion.ts`** (`matchMedia('(prefers-reduced-motion: reduce)')` + change listener).
2. **`TextType.tsx`**: when reduced motion, render first phrase statically, no loop/cursor pulse.
3. **`InteractiveDemo.tsx:319-345`**: reduced motion → render final frame, no auto-cycle.
4. **`FadeContent.tsx`**: reduced motion → render visible immediately.
5. **`HeroBackground.tsx` / `PixelBlast.tsx`**: early-return on reduced motion; wrap `new THREE.WebGLRenderer` (`PixelBlast.tsx:442-446`) in try/catch → return null. Decision: keep mobile-only rendering for now (intentional design) but dynamic-import PixelBlast (`next/dynamic`, `ssr: false`) so desktop never pays for the chunk. If perf still poor on phones, swap to static CSS dot texture (follow-up).

Commit: `fix(site): prefers-reduced-motion support and WebGL failure guard`

## Batch 5 — Broken links, CTAs, commands

1. **Compare tag chips** (`BlogLayout.tsx:62`): when `breadcrumbBase === "compare"`, render tags as plain badges (no link).
2. **Download CTAs**: `BlogLayout.tsx:113` CTA → `/#install` (add `id="install"` to landing TerminalInstall section); MDX footer links in `content/compare/*.mdx` → `/#install`.
3. **Raycast commands** (`page.tsx:357`): `git clone https://github.com/richyparr/tokemon && cd tokemon/tokemon-raycast && npm i && npm run dev`; closing CTA (`page.tsx:432`) → link Raycast section/store instead of `$ npm run dev`.
4. **BMC links** (`BlogLayout.tsx:114-119`): add `target="_blank" rel="noopener noreferrer"`.
5. **`/compare` index** (`compare/page.tsx`): append the same CTA card BlogLayout uses.

Commit: `fix(site): broken tag links, download CTAs, raycast commands`

## Batch 6 — Content accuracy (needs verification during execution)

1. **`claude-code-cost/page.tsx`**: web-verify current Anthropic API pricing + model names (Opus 4.x, Sonnet 4.x, Haiku) and update table (:140-167), FAQ (:53), FAQ JSON-LD (:73-81). Add visible "Last updated: June 2026". Remove "calculator" from meta description/keywords (:10,21) — do NOT build a calculator now (YAGNI; blog post owns that query).
2. **`content/compare/tokemon-vs-ccusage.mdx`**: re-verify rows against ccusage docs; change "Real-time monitoring: No" / "Burn rate: No" → "Limited — `blocks --live` terminal view".
3. **`content/compare/tokemon-vs-claudebar.mdx`**: link the actual ClaudeBar product; verify the seven "No" rows.
4. **`content/compare/tokemon-vs-manual-tracking.mdx`**: replace "15–30 minutes per day" math with the alerting argument; align terminology to "Claude Code usage / plan limits".
5. **Statusline fish gap** (`claude-statusline/page.tsx`): add fish snippet or soften claim at :123/:213.
6. **Keychain step ref** (`keychain-access/page.tsx:106`): "step 6" → "steps 6–8".

Commit: `fix(content): pricing accuracy, ccusage/claudebar fairness, copy fixes`

## Batch 7 — Structured data & OG

1. **`layout.tsx`**: remove `potentialAction` SearchAction (:112-114); `softwareVersion` → read from a new `src/lib/version.ts` constant set to current release (4.1.10).
2. **`blog/[slug]/page.tsx:208-223`**: delete step-less HowTo JSON-LD.
3. **OG images**: keep `/api/og` route; delete `blog/[slug]/opengraph-image.tsx` and `compare/[slug]/opengraph-image.tsx`; ensure twitter + og both point at `/api/og?...`. Add title clamp (~70 chars) + description clamp in `api/og/route.tsx`; switch `runtime = "edge"` → Node.
4. **`blog/[slug]/page.tsx:164`**: support optional `updated` frontmatter → `dateModified`.

Commit: `fix(seo): structured data cleanup, single OG pipeline`

## Batch 8 — Blog polish

1. **Tag taxonomy**: normalize all `content/blog/*.mdx` + `content/compare/*.mdx` tags to kebab-case set (~6: `claude-code`, `usage-tracking`, `rate-limits`, `costs`, `tooling`, `tokemon`); slugify in `blog.ts` `getAllTags()`; display humanized labels in BlogLayout/tag pages.
2. **Heading links**: `next.config.mjs:14` `behavior: "wrap"` — first confirm visually; fix with prose overrides in `BlogLayout.tsx:70`: `prose-headings:no-underline [&_:is(h2,h3)_a]:text-inherit [&_:is(h2,h3)_a]:no-underline` (or switch to `behavior: "append"`).
3. **Reading time**: word-count in `blog.ts` `getPosts()`/`getPost()` (`Math.ceil(words/220)`), render "X min read" next to dates in index + BlogLayout.
4. **Date rendering**: pass `timeZone: "UTC"` to `toLocaleDateString` (`blog/page.tsx:38`, `BlogLayout.tsx:18`).
5. **`BlogLayout.tsx:78`**: Related Posts `h3` → `h2`. Keep blog index on the body gradient (drop `bg-[#0a0a0a]` at `blog/page.tsx:24`).

Commit: `fix(blog): tag taxonomy, heading anchors, reading time, dates`

## Batch 9 — Remaining polish + infra

1. **Security headers** in `next.config.mjs` `headers()`: `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Strict-Transport-Security`, `X-Frame-Options: DENY`.
2. **A11y bits**: `aria-hidden="true"` on FAQ `+` spans (`claude-code-cost:284`, `keychain-access:85`, statusline equivalents); `scope="col"` on pricing `<th>`s; remove `role="img"` from `InteractiveDemo.tsx:381` (keep aria-label on a region, aria-hide decorative internals); skip-to-content link in SiteNav.
3. **Hero h1 height** (`page.tsx:206`): px heights → `min-h-[2.4em] sm:min-h-[1.2em]`-style em values matched to actual line counts per breakpoint.
4. **Landing low-sev**: replace emoji feature icons with inline SVGs (`page.tsx:103-110`); replace star badge (`page.tsx:225-230`) with live GitHub star count (build-time fetch, fallback to plain text) or remove.

Commit: `fix(site): security headers, a11y polish, hero sizing, icons`

---

## Verification (after all batches)

- `npm run build` clean; `npx playwright test` (existing e2e: landing, blog, blog-seo, hero-layout-stability) green.
- Manual: 375px pass over `/`, one blog post, one compare page, `/claude-code-cost` — no horizontal clipping, install command copyable, tables scroll.
- `/definitely-not-a-page`, `/blog/bad-slug`, `/compare/bad-slug` → branded 404.
- Validate one post + one compare page in Google Rich Results test (no HowTo/SearchAction errors).
- Confirm OG images via local `/api/og?...` render with long titles.

## Deliberate decisions (flag if you disagree)

- **No calculator built** — claim removed from meta instead (blog post targets that query).
- **PixelBlast kept mobile-only** but dynamic-imported + guarded; static-texture swap deferred.
- **Single shared nav imported per-page** rather than in root layout (landing hero overlays the nav; restructuring layout is riskier than the duplication it removes via one component).
