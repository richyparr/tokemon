---
phase: 23-seo-content-marketing
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - tokemon-site/content/blog/avoid-claude-rate-limits.mdx
  - tokemon-site/content/blog/claude-token-monitoring-guide.mdx
  - tokemon-site/src/app/blog/[slug]/page.tsx
  - tokemon-site/src/app/blog/[slug]/opengraph-image.tsx
  - tokemon-site/src/app/sitemap.ts
  - tokemon-site/src/components/Nav.tsx
  - tokemon-site/src/lib/blog.ts
  - tokemon-site/src/app/page.tsx
autonomous: true
requirements: [SEO-03, SEO-04, SEO-05, SEO-07]

must_haves:
  truths:
    - "Each blog post has correct meta tags (title, description, openGraph article type)"
    - "Each blog post has a dynamically generated Open Graph image"
    - "Each blog post has Article JSON-LD structured data"
    - "Sitemap includes /blog and all blog post URLs"
    - "Navigation bar includes a Blog link"
    - "Two additional blog posts are published and accessible"
    - "Landing page headings target primary keywords and link to blog"
  artifacts:
    - path: "tokemon-site/content/blog/avoid-claude-rate-limits.mdx"
      provides: "Blog post targeting rate limit search queries"
      min_lines: 40
    - path: "tokemon-site/content/blog/claude-token-monitoring-guide.mdx"
      provides: "Blog post targeting token monitoring search queries"
      min_lines: 40
    - path: "tokemon-site/src/app/blog/[slug]/opengraph-image.tsx"
      provides: "Dynamic OG image generation per blog post"
      exports: ["default", "size", "contentType"]
    - path: "tokemon-site/src/app/sitemap.ts"
      provides: "Sitemap including blog post URLs"
      contains: "blog"
    - path: "tokemon-site/src/components/Nav.tsx"
      provides: "Navigation with Blog link"
      contains: "/blog"
  key_links:
    - from: "tokemon-site/src/app/blog/[slug]/page.tsx"
      to: "JSON-LD script tag"
      via: "Article structured data in page output"
      pattern: "Article.*schema.org"
    - from: "tokemon-site/src/app/blog/[slug]/opengraph-image.tsx"
      to: "tokemon-site/content/blog/*.mdx"
      via: "dynamic import for metadata"
      pattern: "import.*content/blog"
    - from: "tokemon-site/src/app/sitemap.ts"
      to: "tokemon-site/src/lib/blog.ts"
      via: "getPostSlugs for sitemap entries"
      pattern: "getPostSlugs"
    - from: "tokemon-site/src/app/page.tsx"
      to: "/blog"
      via: "internal links from landing page to blog"
      pattern: "href.*blog"
---

<objective>
Write two additional SEO-targeted blog posts, add Article JSON-LD and dynamic OG images to blog post pages, update the sitemap to include all blog URLs, add a Blog link to the navigation bar, and optimize landing page copy for SEO keywords with internal linking to blog content.

Purpose: Maximizes search visibility with structured data, OG images for social sharing, sitemap coverage for Google indexing, content volume targeting high-intent Claude usage keywords, and landing page keyword optimization.
Output: Three total blog posts with full SEO metadata, dynamic OG images, sitemap coverage, navigation integration, and SEO-optimized landing page copy.
</objective>

<execution_context>
@/Users/richardparr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardparr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-seo-content-marketing/23-RESEARCH.md
@.planning/phases/23-seo-content-marketing/23-01-SUMMARY.md

Note: Ahrefs MCP is not configured in this project. Content keyword targeting is based on competitor analysis and inferred search intent from RESEARCH.md rather than Ahrefs keyword volume data. If the user wants to configure Ahrefs MCP before execution, run: `npx @anthropic-ai/claude-code mcp add ahrefs-mcp` (see https://github.com/ahrefs/ahrefs-mcp-server).

<interfaces>
<!-- From Plan 01 — blog infrastructure contracts -->

From tokemon-site/src/lib/blog.ts (created in Plan 01):
```typescript
export type BlogPostMetadata = {
  title: string;
  description: string;
  date: string;
  author: string;
  tags: string[];
};
export function getPostSlugs(): string[];
export function getPosts(): Promise<Array<BlogPostMetadata & { slug: string }>>;
```

From tokemon-site/content/blog/*.mdx (pattern from Plan 01):
```typescript
// Each MDX file exports metadata object:
export const metadata = {
  title: "...",
  description: "...",
  date: "YYYY-MM-DD",
  author: "Richard Parr",
  tags: ["..."],
};
```

From tokemon-site/src/components/Nav.tsx:
```typescript
// Fixed nav with logo, GitHub link, Download button
// Needs Blog link added between logo and GitHub
export function Nav() { ... }
```

From tokemon-site/src/app/layout.tsx:
```typescript
// Root metadata includes metadataBase: new URL("https://tokemon.ai")
// Blog post pages will merge metadata via generateMetadata
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write two additional blog posts and add Blog nav link</name>
  <files>tokemon-site/content/blog/avoid-claude-rate-limits.mdx, tokemon-site/content/blog/claude-token-monitoring-guide.mdx, tokemon-site/src/components/Nav.tsx, tokemon-site/src/lib/blog.ts</files>
  <action>
1. Write `content/blog/avoid-claude-rate-limits.mdx`:
   - metadata: title "How to Avoid Claude Rate Limits: A Developer's Guide", description targeting "avoid claude rate limits", "claude rate limit workaround", "claude code rate limit" keywords, date "2026-03-12", tags ["claude-code", "rate-limits", "tips"]
   - Content (~800-1200 words): Explain what Claude rate limits are (5-hour rolling window, model-dependent caps), why they happen (heavy usage bursts, large context windows), practical strategies to avoid them (monitor usage in real-time with Tokemon, spread work across sessions, use smaller models for simple tasks, reduce context window size), and what to do when you hit one (wait for reset, check remaining capacity via Tokemon's burn rate). Include code examples or terminal output showing what a rate limit error looks like.
   - CTA: Download Tokemon to monitor usage and avoid surprise rate limits.

2. Write `content/blog/claude-token-monitoring-guide.mdx`:
   - metadata: title "The Complete Guide to Claude Token Usage Monitoring", description targeting "claude token usage", "monitor claude tokens", "claude usage analytics" keywords, date "2026-03-14", tags ["claude-code", "tokens", "monitoring", "analytics"]
   - Content (~800-1200 words): Cover what tokens are in Claude's context (input/output), how token usage maps to rate limits and costs, why monitoring matters (avoid interruptions, budget tracking, project cost allocation), tools available (Tokemon for macOS/Raycast, ccusage for CLI), deep dive into Tokemon's monitoring capabilities (real-time burn rate, per-project breakdown, team budgets, export for reporting), setup guide.
   - Include a comparison table: Tokemon vs manual tracking vs ccusage.
   - CTA: Get started with Tokemon.

3. Update `src/components/Nav.tsx` to add a "Blog" link:
   - Add a text link "Blog" pointing to "/blog" in the nav items, positioned before the GitHub link
   - Style it the same as the GitHub link: `text-secondary-text hover:text-[#ededed] transition-colors hidden sm:inline`
   - Use Next.js `Link` component (import from "next/link") for client-side navigation

4. If `src/lib/blog.ts` uses a hardcoded post list (rather than fs.readdirSync), update it to include all three slugs. If it uses dynamic directory reading, verify it picks up the new files by running `npm run build`.
  </action>
  <verify>
    <automated>cd /Users/richardparr/Tokemon/tokemon-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Three blog posts exist in content/blog/ and render correctly. Nav bar includes a Blog link. Blog index shows all three posts sorted by date.</done>
</task>

<task type="auto">
  <name>Task 2: Add Article JSON-LD, dynamic OG images, and update sitemap</name>
  <files>tokemon-site/src/app/blog/[slug]/page.tsx, tokemon-site/src/app/blog/[slug]/opengraph-image.tsx, tokemon-site/src/app/sitemap.ts</files>
  <action>
1. Update `src/app/blog/[slug]/page.tsx` to add Article JSON-LD structured data:
   - After importing the MDX module's metadata, construct an Article JSON-LD object:
     ```typescript
     const articleJsonLd = {
       "@context": "https://schema.org",
       "@type": "Article",
       headline: metadata.title,
       description: metadata.description,
       datePublished: metadata.date,
       dateModified: metadata.date,
       author: { "@type": "Person", name: "Richard Parr", url: "https://github.com/richyparr" },
       publisher: { "@type": "Organization", name: "Tokemon", url: "https://tokemon.ai" },
       mainEntityOfPage: `https://tokemon.ai/blog/${slug}`,
     };
     ```
   - Render as `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(articleJsonLd) }} />` inside the page component, before the article content.

2. Create `src/app/blog/[slug]/opengraph-image.tsx` for dynamic OG image generation:
   - Export `size = { width: 1200, height: 630 }` and `contentType = "image/png"`
   - Export `generateStaticParams` returning the same slugs as the page
   - Default export: async function that dynamically imports the MDX metadata, then returns `new ImageResponse(...)` from `next/og`
   - Design: Dark background (#0a0a0a), orange accent text "tokemon.ai/blog" at top, white bold title text (56px), gray description text below, consistent with the site's dark theme
   - DO NOT try to load custom fonts (Geist) — use the system default. Custom font loading in OG images requires fetching font files and is unnecessary for the MVP.

3. Update `src/app/sitemap.ts` to include blog URLs:
   - Import `getPostSlugs` from `@/lib/blog`
   - Keep the existing homepage entry
   - Add a /blog index entry (priority 0.8, weekly changeFrequency)
   - Add an entry for each blog post slug (priority 0.7, monthly changeFrequency)
   - URL format: `https://tokemon.ai/blog/${slug}`

4. Verify: Run `npm run build` and check that the sitemap output includes blog URLs. Check that the OG image route generates without errors.
  </action>
  <verify>
    <automated>cd /Users/richardparr/Tokemon/tokemon-site && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Each blog post page includes Article JSON-LD structured data in a script tag. Dynamic OG images generate for each blog post at /blog/[slug]/opengraph-image. Sitemap at /sitemap.xml includes homepage, /blog, and all individual blog post URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Landing page SEO copy review and internal linking</name>
  <files>tokemon-site/src/app/page.tsx</files>
  <action>
1. Read the existing landing page at `src/app/page.tsx` and review the heading hierarchy and copy for SEO keyword targeting.

2. Optimize heading tags for primary keywords:
   - Ensure the h1 contains the primary keyword "Claude Code Usage Monitor" or "Claude Usage Monitor" (the existing h1 likely says "Tokemon" — append or rephrase to include the primary search term, e.g., "Tokemon — Claude Code Usage Monitor for macOS")
   - Ensure h2 headings in the features section use keyword-rich phrasing (e.g., "Real-Time Token Tracking" instead of generic "Features", "Monitor Claude Rate Limits" instead of "Alerts")
   - Keep changes minimal and natural-sounding — do not keyword-stuff

3. Add internal links from the landing page to blog content:
   - In the features section or below it, add a small "From the Blog" or "Learn More" section with 2-3 links to the blog posts:
     - "How to Track Claude Code Usage" -> /blog/how-to-track-claude-code-usage
     - "Avoid Claude Rate Limits" -> /blog/avoid-claude-rate-limits
   - Style consistently with the existing dark theme. Use subtle link styling (text-[#e8853b] or similar accent color).
   - Alternatively, add contextual inline links within existing feature descriptions that naturally point to relevant blog posts.

4. Do NOT restructure the page layout, change the hero design, or modify the interactive demo. Only touch copy text and add internal links.

5. Verify: `npm run build` succeeds and existing landing-page E2E tests still pass.
  </action>
  <verify>
    <automated>cd /Users/richardparr/Tokemon/tokemon-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page h1 includes primary keyword "Claude Code Usage Monitor". Feature headings use keyword-rich phrasing. Internal links to blog posts are present on the landing page. No layout or design changes. Build succeeds and existing E2E tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- /blog lists all 3 posts sorted by date (newest first)
- Each blog post renders with full MDX content
- Nav bar shows "Blog" link that navigates to /blog
- /sitemap.xml includes /blog and all blog post URLs
- Blog post pages include `<script type="application/ld+json">` with Article schema
- /blog/[slug]/opengraph-image generates a 1200x630 PNG
- Landing page h1 targets primary keyword
- Landing page has internal links to blog posts
- Existing landing-page.spec.ts E2E tests still pass
</verification>

<success_criteria>
- Three SEO-targeted blog posts published and accessible
- Article JSON-LD on every blog post page
- Dynamic OG images generated per blog post for social sharing
- Sitemap updated with all blog URLs for Google indexing
- Blog link added to site navigation
- Landing page copy optimized with keyword-targeted headings and internal links to blog
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-seo-content-marketing/23-02-SUMMARY.md`
</output>
