---
phase: ad-hoc (bug fixes + SEO)
status: completed
last_updated: 2026-03-09
---

<current_state>
All work committed and pushed to main. No in-progress tasks.
</current_state>

<completed_work>

## Tokemon App (commit f1bf67b)
- Fix Team tab 404: changed /members to /users endpoint
- Fix Team tab 400: removed invalid group_by[]=user_id
- Fix TeamMember model (CodingKeys, cursor pagination)
- Add Total Spent tile + per-member cost via Claude Code Analytics API
- Add role badges, graceful 429 handling, background cost fetch
- PDF export: determinate progress bar, cancel button, error/retry UI
- Multi-page PDF via PDFReportBuilder
- 30s timeout on all Admin API requests
- NSSavePanel.level = .floating on all save dialogs
- Add ClaudeCodeAnalytics.swift model + fetchClaudeCodeCostByUser method

## SEO (commits 8ef97c8, e1cc33c)
- FAQ schema + visible FAQ section on homepage
- Canonical URLs on all pages
- /compare index page
- Organization + WebSite JSON-LD schemas
- Breadcrumb schema on blog + compare pages
- Publisher logo in Article schemas
- Sitemap uses actual content dates
- 5 new blog posts (plans comparison, cost reduction, rate limits, cost calculator, tools)
- 2 new comparison pages (vs Console, vs manual tracking)
- Compare link in nav + footer
- Site: 8 → 14 indexable pages
</completed_work>

<remaining_work>

- Team tab $0.00 cost: usage/cost APIs returning zero — may be rate limited from testing, needs retry after cooldown
- Commit MemberUsageRow.swift deletion (now unused, replaced by MemberRow in TeamDashboardView)
- Off-site SEO: Product Hunt, GitHub README link, directories, Reddit post (reddit-draft.md exists)
- Verify sitemap submitted in Google Search Console (user was doing this manually)
</remaining_work>

<decisions_made>

- group_by[]=user_id is NOT valid in Anthropic Usage API — per-user cost uses Claude Code Analytics API instead
- Per-user cost fetched in background Task so it doesn't block UI loading
- 429 on Claude Code Analytics returns partial data instead of erroring
- All data fetches are independent — members, usage, cost, per-user cost fail silently
- FAQ data shared between JSON-LD schema and visible HTML section
</decisions_made>

<blockers>
- Admin API rate limited from rapid testing — wait ~10 min before retrying Team tab
</blockers>

<next_action>
1. Wait for rate limit to clear, then test Team tab cost/token data
2. If still $0.00, debug with curl to inspect raw API responses
3. Off-site SEO tasks (Product Hunt launch, README, Reddit)
</next_action>
